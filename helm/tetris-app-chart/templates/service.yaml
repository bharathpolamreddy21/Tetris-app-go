apiVersion: v1
kind: Service
metadata:
  name: tetris-service
  namespace: tetris-ns
spec:
  selector:
    app: tetris-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort